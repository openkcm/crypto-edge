{{- if .Values.tenantManager.pod.disruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "crypto-edge.fullname" . }}-tenant-manager
  namespace: {{ include "crypto-edge.namespace" . }}
  labels:
    {{- include "crypto-edge.tenant-manager.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "crypto-edge.tenant-manager.selectorLabels" . | nindent 6 }}
  {{- if not (or (hasKey .Values.tenantManager.pod.disruptionBudget "minAvailable") (hasKey .Values.tenantManager.pod.disruptionBudget "maxUnavailable")) }}
  minAvailable: 1 # Default value because minAvailable and maxUnavailable are not set
  {{- end }}
  {{- if (and (hasKey .Values.tenantManager.pod.disruptionBudget "minAvailable") (hasKey .Values.tenantManager.pod.disruptionBudget "maxUnavailable")) }}
  {{- fail "You can only set one of minAvailable or maxUnavailable in the PodDisruptionBudget" }}
  {{- end }}
  {{- if hasKey .Values.tenantManager.pod.disruptionBudget "minAvailable" }}
  minAvailable: {{ .Values.tenantManager.pod.disruptionBudget.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.tenantManager.pod.disruptionBudget "maxUnavailable" }}
  maxUnavailable: {{ .Values.tenantManager.pod.disruptionBudget.maxUnavailable }}
  {{- end }}
{{- end }}
