{{- if .Values.common.serviceAccount.create -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "crypto-edge.serviceAccountName" . }}
  namespace: {{ include "crypto-edge.namespace" . }}
  labels:
    {{- include "crypto-edge.labels" . | nindent 4 }}
  {{- with .Values.common.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.common.serviceAccount.automount }}
{{- end }}
