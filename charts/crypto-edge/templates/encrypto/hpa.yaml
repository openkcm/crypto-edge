{{- if .Values.encrypto.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "crypto-edge.fullname" . }}
  namespace: {{ include "crypto-edge.namespace" . }}
  labels:
    crypto-edge.openkcm.io/component: encrypto
    {{- include "crypto-edge.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "crypto-edge.fullname" . }}
  minReplicas: {{ .Values.encrypto.hpa.minReplicas }}
  maxReplicas: {{ .Values.encrypto.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.encrypto.hpa.targetCPUUtilizationPercentage }}  # Target 70% CPU usage per pod
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.encrypto.hpa.targetMemoryUtilizationPercentage }}  # Target 80% Memory usage per pod
{{- end }}
